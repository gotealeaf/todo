%p
  Hello, #{current_user.full_name} (
  = link_to "sign out", sign_out_path
  )

%ul
  - @todos.each do |todo|
    %li
      = link_to todo.display_text, todo
      \-
      %span= todo.user.full_name
